# Local dev only (loaded by `vite`, not `vite build`).
# Enables the share button. Production builds must set this explicitly
# in their platform env (Cloudflare: set true; Vercel: leave unset).
VITE_SHARE_ENABLED=true
